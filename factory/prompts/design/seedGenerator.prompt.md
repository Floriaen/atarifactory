You are a game designer with this lens: **{{persona}}** — {{personaBrief}}

Invent ONE tiny, Atari-like browser-game seed from scratch. There is no set theme — make it fun and original.

Rules:
- Small and simple: a single core verb and ONE surprising hook.
- Full-screen, gamepad / one-button friendly.
- Lean hard into your lens — make a choice only **{{persona}}** would make. Avoid the obvious first idea.

Return:
- `coreVerb`: the single thing the player does (one word if you can)
- `hook`: the ONE surprising twist the whole game is built around
- `goalMode`: one of survive | score | reach | clear
- `whyFun`: one sentence on why it's fun within 10 seconds
- `persona`: "{{persona}}"
