Write a vanilla JavaScript game using only the HTML5 Canvas API (no frameworks, no external libraries).

Requirements:
- Title: {{title}}
- Description: {{description}}
- Mechanics: {{mechanics}}
- Win condition: {{winCondition}}
- The canvas element has id='game-canvas'. Always use document.getElementById('game-canvas').
- Use only the Canvas API for all drawing and game logic.
- Do NOT use any frameworks or external libraries.
- The game should be single-screen and mobile-friendly.
- Display the score and game over messages on the screen using Canvas text.
- Output only the code for the main game logic (game.js), no explanations.
- The code should be self-contained and run in a browser. 